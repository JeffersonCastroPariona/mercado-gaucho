runtime: nodejs18

env: flex

instance_class: F2  # Clase de instancia, puedes cambiarla según tus necesidades

handlers:
  - url: /assets
    static_dir: dist/assets
  - url: /.*   
    static_files: dist/index.html
    upload: dist/index.html
